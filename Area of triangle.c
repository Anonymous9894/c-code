#include<stdio.h>
#include<conio.h>
void main()
{
	float l,h,a;
	printf("enter the length and height of triangle=\n");
	scanf("%f%f",&l,&h);
	a=0.5*l*h;
	printf("Area of triangle=%f",a);
	getch();
	
}
